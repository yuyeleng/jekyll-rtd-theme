{%- include common/rest/workdir.liquid %}
{%- assign dir = site_pages | where: "url", workdir | first -%}
{% assign dirUrl = dir.url | split: "/" %}{% comment %} 截取目录文件夹的路径，这个和头部导航的相关联 {% endcomment %}
<div class="header-box">
    <div class="logo-box">
        <img class="logo" src="../../assets/img/logo.png" />
    </div>
    <ul class="nav-box">
        <li class="nav-item active">
            <a>现货</a>
        </li>
        <li class="nav-item">
            <a>合约</a>
        </li>
        <li class="nav-item">
            <a>交割合约</a>
        </li>
        {%- for workdir in workdir_dirs %}{% include templates/header_menu.liquid %}{% endfor -%}
        
    </ul>
</div>